# Cross-repo compatibility refs consumed by provider compatibility lanes.
# FluxGraph is intentionally floating (`main`) while provider/graph integration
# is still evolving.
# Revisit immutable SHA pinning before release hardening.

fluxgraph:
  repository: anolishq/fluxgraph
  ref: v1.0.2
  updated_utc: 2026-04-21
  rationale: "Pinned to v1.0.2 release. Linux CI uses this ref to build fluxgraph-server for test execution."
